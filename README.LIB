To set up the libraries and utilities (as user):

cd $HOME/OpenFOAM/$USER-$OFVER
mkdir applications run
cd applications
git clone https://github.com/vitst/libsFoamAux


cd libsFoamAux
mkInclude
Allwmake

Note: Solvers and utilities are installed in parallel directories
